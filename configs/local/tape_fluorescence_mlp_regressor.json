{
    "seed": 42,
    "data": {
        "train_path": "s3://data/tape/fluorescence/train.csv",
        "valid_path": "s3://data/tape/fluorescence/valid.csv",
        "dataset_type": "one_hot"
    },
    "model": {
        "name": "mlp_regressor",
        "params": {
            "layer_dims": [
                1024,
                1
            ],
            "hidden_activation": "LeakyReLU",
            "output_activation": "Identity",
            "use_bias": true,
            "norm": "layer"
        }
    },
    "training": {
        "experiment_name": "tape_fluorescence_regression",
        "run_name": null,
        "model_path": "s3://models/tape_fluorescence_mlp_regressor",
        "lr": 0.0001,
        "weight_decay": 0.01,
        "max_epochs": 5,
        "batch_size": 256,
        "val_frequency": 1,
        "patience": 25,
        "tracking": false
    }
}